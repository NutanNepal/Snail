<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Snail_day" parent="Theme.Material3.Dark.NoActionBar">

        <!-- Original AppCompat attributes. -->
        <item name="colorPrimary">@android:color/system_primary_light</item>
        <item name="colorPrimaryDark">@android:color/system_primary_dark</item>
        <item name="colorSecondary">@android:color/system_secondary_light</item>
        <item name="android:colorBackground">@color/launcher</item>
        <item name="colorError">@android:color/system_error_light</item>

        <!-- Status bar color. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>


        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>
        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/fade_in</item>
        <item name="android:windowExitTransition">@transition/fade_out</item>

        <!-- specify shared element transitions-->
        <item name="android:windowSharedElementEnterTransition">@transition/fade_in</item>
        <item name="android:windowSharedElementExitTransition">@transition/fade_out</item>


    </style>
</resources>